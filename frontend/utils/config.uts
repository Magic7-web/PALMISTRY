/**
 * API 配置
 * API Key 由后端 server/ 代理持有，前端仅请求 /api/qwen
 */

export const QWEN_CONFIG = {
	/** H5 开发与生产均走 /api/qwen（Vite proxy 或 Nginx 反代到 server） */
	endpoint: '/api/qwen',
	model: 'qwen3.7-flash',
	timeout: 120000 // 手相多模态分析 + 长输出，预留 2 分钟
};
